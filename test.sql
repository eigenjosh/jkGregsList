-- Animals
-- CREATE TABLE jkanimals
-- (
--     id int NOT NULL
--     AUTO_INCREMENT,
--      name VARCHAR
--     (255),
--      description VARCHAR
--     (255), 
--      price DOUBLE
--     (40,2), 
--      PRIMARY KEY
--     (id)
--      );

    -- SELECT *
    -- FROM jkanimals

    -- SELECT *
    -- FROM jkanimals
    -- WHERE id = 1 OR id = 2;

--     UPDATE jkanimals SET  
--      description = 'A super old burgercat!';
--  WHERE id = 2;
--     SELECT *
--     FROM jkanimals
--     WHERE id = 2;

-- INSERT INTO jkanimals (Name, Description, Price)
-- VALUES ('Burgercat', 'Really old', '0.45');

-- Autos

-- CREATE TABLE jkautos
-- (
--     id int NOT NULL
--     AUTO_INCREMENT,
--      name VARCHAR
--     (255),
--      description VARCHAR
--     (255), 
--      price DOUBLE
--     (40,2), 
--      PRIMARY KEY
--     (id)
--      );

--     SELECT *
--     FROM jkautos

--     SELECT *
--     FROM jkautos
--     WHERE id = 1 OR id = 2;

--     UPDATE jkautos SET  
--      description = 'A super old burgercat!';
--  WHERE id = 2;
--     SELECT *
--     FROM jkautos
--     WHERE id = 2;

-- INSERT INTO jkautos (Name, Description, Price)
-- VALUES ('Burgercar', 'Really old', '45.00');

-- Properties

-- CREATE TABLE jkproperties
-- (
--     id int NOT NULL
--     AUTO_INCREMENT,
--      name VARCHAR
--     (255),
--      description VARCHAR
--     (255), 
--      price DOUBLE
--     (40,2), 
--      PRIMARY KEY
--     (id)
--      );

--     SELECT *
--     FROM jkproperties

--     SELECT *
--     FROM jkproperties
--     WHERE id = 1 OR id = 2;

--     UPDATE jkproperties SET  
--      description = 'A super old burgercat!';
--  WHERE id = 2;
--     SELECT *
--     FROM jkproperties
--     WHERE id = 2;

-- INSERT INTO jkproperties (Name, Description, Price)
-- VALUES ('Burgercondo', 'Really old', '450.00');